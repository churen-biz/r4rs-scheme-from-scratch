# 给实现者与改文档的人

本仓库的早期层代码是手写 `.s`；自托管之后才出现 Scheme 编译器源。你若在实现中发现文档与可运行系统对不上，请改文档或开 issue——不要默默猜。不要用 Python / Chez / Guile 另写一套代码生成器来「先跑起来」。

## 反馈循环

```
读第 N 层 → 写最小实现 → 跑该层测例 + 回归 → 若文档有洞或错
    → 记下「哪一句导致误实现」→ 改该层 markdown（或 ARCHITECTURE.md）
    → 再跑测例
```

优先改文档，而不是在实现里绕过文档。教程的目标是：**只靠文档就能做出可测系统**。

## 报告「文档 vs 现实」不一致

开 issue 或 PR 时请包含：

1. 层号（如 `L07`）和文件路径。
2. 测例编号（如 `3. (fx- 3 10)` 期望 `-7`）。
3. 你按文档做出的行为 vs 你认为正确的行为。
4. 目标机：默认 `aarch64-apple`；若你在别的芯片上，写明 triple。
5. 是标签/IR 合同问题（应改 `ARCHITECTURE.md`）还是该层叙述问题。

不要只说「跑不通」。没有复现路径的反馈很难被吸收进文档。

## 层文档修订约定

- **只改一层能解决的，就只改那一层。** 标签、寄存器、IR 形状变了，必须同步 `ARCHITECTURE.md`、`backend/README.md`，并在受影响层的「与上一层的差异」加一句。
- **禁止**把新能力偷偷写进旧层测例。新行为 = 新层或该层明确的新测例。
- 结构必须保持八节，顺序不变：

  `目标 / 原理 / 与上一层的差异 / 代码骨架 / 测例清单 / 验收标准 / 常见坑 / 下一层预告`

- 删除某测例要在 PR 说明里写原因。新增测例用下一个序号，不要插入后把后面全部改号（除非该层还没人实现）。
- 中文叙述；标识符、Scheme 形式、助记符、路径保持英文。
- 不要写「TBD」「TODO」「稍后补充」。若本层不做某事，用「本层范围之外：…」写死。
- 预告只指出下一层要解决的**一个问题**，不要剧透实现。

## 测试命名

```
tests/Lxx/NNN-short-english-slug.scm
tests/Lxx/NNN-short-english-slug.expected    # 若驱动用旁路文件
```

- `Lxx` 与文档文件名中的编号一致（`layers/L07-binary-arithmetic.md` → `tests/L07/`）。
- `NNN` 三位十进制，与该层「测例清单」列表序号对齐：`1.` → `001`。
- slug 用 kebab-case 英文：`fx-sub-negative.scm`，不要用中文文件名。
- 错误测例 slug 带 `err-` 前缀：`009-err-div-zero.scm`。
- 回归不复制旧测例文件；驱动按层号 `0..N` 扫描。

期望输出默认是 `_rt_print` 的一行（含换行）。二进制、空输出、非零退出码在该测例条目里写明。

实现时只提交 `.s` 以及 `Makefile` / shell 胶水。不要添加 `runtime.c` / `scheme.h` / 任何 `.py`。自托管阈值之后才允许 Scheme 编译器源（`.scm`），且不得用其它高级语言 emit 汇编。

## PR 标题

- 文档：`docs(L12): 澄清 HP 在 scheme_entry 中的保存`
- 实现：`feat(L12): bump allocator`（手写 `.s`；自托管后才是 Scheme 编译器）

## 许可

贡献视为在 MIT License 下提交（见 README）。
